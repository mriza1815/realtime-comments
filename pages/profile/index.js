import React from 'react';
import PropTypes from 'prop-types';

const Profile = props => {
  console.log("Profile", props.url.query.id)

  return (
    <div>
      
    </div>
  );
};

Profile.propTypes = {
  
};

export default Profile;